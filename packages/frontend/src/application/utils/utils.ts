export const appUtils = {
  // ensIsPersona(record: string) {
  //   const arr = record.split(",");
  //   for (let i = 0; i < arr.length; i++) {
  //     const text = arr[i];
  //     if (guilds.includes(text as any)) return true;
  //   }
  //   return false;
  // },
};
